# Opacity
inactive-dim = 0.15;

# Fading
fading = true;
fade-delta = 4;
fade-in-step = 0.03;
fade-out-step = 0.03;
no-fading-openclose = false;

# Other
use-ewmh-active-win = true;
mark-ovredir-focused = true;
